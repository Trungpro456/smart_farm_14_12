import { Button } from "@/components/ui/button";

const Main = () => {
  return (
    <div>
      <Button onClick={() => console.log("Api call")}>Api call</Button>
    </div>
  );
};
export default Main;
